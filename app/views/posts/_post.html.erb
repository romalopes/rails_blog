<div id="<%= dom_id post %>">
  <p>
    <h2><%= post.title %></h2>
    <h4>Posted by: <%= link_to "#{post.user.name}", user_path(post.user.id)  %></h4>
  </p>
  <p>
    <h6><%= pluralize(post.views, "view") %></h6>
  </p>

  <p>
    <%= post.body %>
  </p>

</div>
